version: '3.8'

services:
  web:
    container_name: portal_web
    ports:
      - "80:80"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    restart: always
    networks:
      - portal_net

  ngrok:
    image: ngrok/ngrok:latest
    container_name: portal_ngrok
    command:
      - "http"
      - "web:80"
      - "--domain=${NGROK_DOMAIN}"
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    restart: always
    networks:
      - portal_net

networks:
  portal_net:
    driver: bridge
